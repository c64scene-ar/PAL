#ifndef __GEN_ROOMS_H__
#define __GEN_ROOMS_H__

/* This is an autogenerated file. Do not modify directly! */

<% rooms.each do |room_id, attrs| %>
  static Room <%= room_id %> = {
      /*.name         = */ <%= string(attrs['name'] || room_id) %>,
      /*.prefix       = */ <%= string(attrs['prefix']) %>,
      /*.alias        = */ <%= string(attrs['alias']) %>,
      /*.description  = */ <%= string(attrs['description']) %>,
      /*.exits        = */ { NULL, NULL, NULL, NULL, NULL, NULL, NULL, NULL },
      /*.objects      = */ NULL,
  };
<% end %>

void connect_rooms(void) {
  <% rooms.each do |from, attrs| %>
    <% exits_from_attrs(attrs).each do |exit_id, to| %>
      <%= from %>.exits[<%= exit_id %>] = &<%= to %>;
    <% end %>
  <% end %>
}

#endif
